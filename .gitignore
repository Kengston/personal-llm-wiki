# Secrets & credentials — NEVER commit
.env
.env.*
!.env.example
*.token
*.secret
*.key
*.pem
secrets/

# Defensive: финансовые данные в публичном репо появиться не должны никогда.
# raw/finance/ живёт исключительно в приватном llm-wiki-content.
# Если папка случайно окажется здесь — git её не подберёт (Phase-0 Safety Gate).
raw/finance/

# Engine auth dirs (subscription OAuth tokens live here)
.codex/
.grok/
.openclaw/

# Local state / databases
*.sqlite
*.sqlite-*
*.db

# Python
__pycache__/
*.pyc
.venv/
venv/

# Node
node_modules/
dist/
*.tsbuildinfo
pnpm-debug.log*
npm-debug.log*

# Runtime logs (bridge/scheduler stdout/stderr — могут содержать chat_id/usage)
logs/
*.log

# OS / editor
.DS_Store
.idea/
.vscode/

# Claude Code — личная агент-оснастка (ревьюер), в публичный портфолио-репо не коммитим
.claude/

# Локальные файлы со значениями (справочник рендера резюме, D3/[ADR-0028]): реальные имя,
# контакты и названия организаций не хранятся ни в одном репозитории. Пример формы —
# career-directory.example.json, он синтетический и трекается.
*.local.json
